The ruler-graduation divider pulled from the wordmark's stem hatching. Use for major section breaks only — roughly once per screen; minor separations stay 1px hairlines.

```jsx
<TickRule />
<TickRule dim />
<div style={{display:'flex'}}><TickRule vertical /> …</div>
```
