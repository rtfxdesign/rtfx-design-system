Sticky blurred site header. Layout rule: the wordmark sits small (~120px) here and no Martian display type appears in the same band.

```jsx
<Topbar logoSrc="assets/rtfx-wordmark-white.svg"
  links={[{label:'Work',active:true},{label:'Services'},{label:'Contact'}]}
  right={<Status state="ok">Taking projects</Status>} />
```
