Project/portfolio card. Thumb falls back to the brand hatch placeholder — pass an `<img style={{width:'100%',height:'100%',objectFit:'cover'}}>` when real media exists.

```jsx
<WorkCard idx="004" status="live" statusLabel="Live" title="Revd Cycling"
  desc="House rig — 44-panel wall, 96 fixtures, one operator surface."
  tags={["LED","DMX",{label:"Show control",accent:true}]} href="#work/revd" />
```
